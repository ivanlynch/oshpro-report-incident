name: AUTOMERGE

on:
  pull_request_target:
    types:
      - labeled
jobs:
  automerge:
    name: AUTOMERGE
    runs-on: ubuntu-latest
    steps:
      - name: automerge
        uses: "pascalgn/automerge-action@v0.12.0"
        env:
          GITHUB_TOKEN: "${{ secrets.GH_TOKEN }}"