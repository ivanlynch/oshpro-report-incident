name: "RELEASE"

on:
  push:
    tags:
      - "v*"

jobs:
  tagged-release:
    name: "TAGGED RELEASE"
    runs-on: "ubuntu-latest"

    steps:
      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GH_TOKEN }}"
          prerelease: false